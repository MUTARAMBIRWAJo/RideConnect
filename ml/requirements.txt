fastapi==0.111.0
uvicorn[standard]==0.29.0
numpy==1.26.4
pydantic==2.7.1
# Use tflite-runtime for lightweight inference (no full TF install needed):
tflite-runtime==2.14.0
# If tflite-runtime is unavailable for your platform, fall back to:
# tensorflow==2.16.1
